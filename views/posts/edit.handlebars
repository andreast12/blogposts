<h1 class="mt-3">Edit Post</h1>

<form action="/posts/edit/{{post._id}}?_method=put" method="post" class="mt-4">
  <div class="form-floating mb-4">
    <input type="text" class="form-control" id="inputTitle" placeholder="dummy" name="title" required
      value="{{post.title}}">
    <label for="inputTitle" class="form-label">Title</label>
  </div>
  <div class="form-floating mb-4">
    <textarea class="form-control" id="inputBody" placeholder="dummy" style="height: 200px;" name="body"
      required>{{post.body}}</textarea>
    <label for="inputBody" class="form-label">Body</label>
  </div>

  <div class="d-flex column-gap-3">
    <input type="submit" value="Save" class="btn btn-primary">
    <a href="/posts/view/{{post._id}}" class="btn btn-secondary">Cancel</a>
  </div>
</form>